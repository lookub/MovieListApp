<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/colorPrimaryDarkTmbd</item>
        <item name="android:textColor">@color/whiteBG</item>
        <item name="android:editTextColor">@color/whiteBG</item>


        <item name="android:alertDialogStyle">@style/MyAlertDialogStyle</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>


    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">

        <item name="android:textColor">@color/whiteBG</item>
        <item name="android:editTextColor">@color/whiteBG</item>
        <item name="android:textColorSecondary">@color/whiteBG</item>
        <item name="android:textColorAlertDialogListItem">@color/whiteBG</item>

        <!-- Used for the buttons -->
        <item name="android:colorAccent">@color/colorAccent</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <!-- Used for the background -->
        <item name="android:windowBackground">@color/colorPrimaryDarkTmbd1</item>

    </style>





</resources>
